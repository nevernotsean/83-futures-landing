{
  "name": "eighty-three-futures",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --no-cache",
    "build": "rm -rf dist/* && rm -rf .cache && parcel build index.html --no-content-hash --no-minify --public-url ./ && npm run zip",
    "zip": "zip -r ./dist/build.zip ./dist/."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "sass": "^1.22.2"
  }
}
